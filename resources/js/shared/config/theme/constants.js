/**
 * Theme configuration constants
 * Layer: shared/config/theme
 */

export const THEME_PREFERENCES = Object.freeze({
  LIGHT: 'light',
  DARK: 'dark',
  SYSTEM: 'system',
});

export const THEMES = Object.freeze({
  LIGHT: 'light',
  DARK: 'dark',
});

export const DEFAULT_THEME_PREFERENCE = THEME_PREFERENCES.SYSTEM;
