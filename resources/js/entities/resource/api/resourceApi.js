/**
 * Resource entity — API calls.
 *
 * Placeholder — will use Http from @shared/api when the real API is ready.
 */
import { RESOURCES } from '../model/fixtures';

/**
 * Fetch resources for a given topic.
 * Currently returns the dummy fixtures; swap for Http.get() call later.
 */
export const fetchResources = async () => RESOURCES;
